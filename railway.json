{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "pnpm build && npx drizzle-kit migrate"
    },
    "deploy": {
        "startCommand": "pnpm start",
        "healthCheckPath": "/health",
        "healthCheckTimeout": 100
    }
}